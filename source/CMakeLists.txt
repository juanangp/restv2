# This file is intentionally minimal. Each subdirectory has its own
# CMakeLists.txt and is added from the root CMakeLists.txt directly:
#
# add_subdirectory(source/tools) add_subdirectory(source/core)
# add_subdirectory(source/bin)
#
# Nothing to do here.
